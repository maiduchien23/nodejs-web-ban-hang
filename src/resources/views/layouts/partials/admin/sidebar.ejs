<aside class="main-sidebar sidebar-dark-primary elevation-4">
	<!-- Brand Logo -->
	<a href="/admin"
		class="brand-link">
		<span class="d-flex align-items-center brand-text font-weight-light">
			
			<h3 class="font-weight-bold mb-0 ml-2">Admin</h3>
		</span>
	</a>

	<!-- Sidebar -->
	<div class="sidebar">
		<!-- Sidebar Menu -->
		<nav class="mt-2">
			<ul class="nav nav-pills nav-sidebar flex-column"
				data-widget="treeview"
				role="menu"
				data-accordion="false">
				<!-- Add icons to the links using the .nav-icon class
   with font-awesome or any other icon font library -->
				<li class="nav-item">
					<a href="/admin"
						class="nav-link <%= moduleName === "Tổng quan" ? "active" : "" %>">
						<i class="nav-icon fas fa-tachometer-alt"></i>
						<p>Tổng quan</p>
					</a>
				</li>
				<% if(permissionUtils.getPermissionUser(permissionUser, 'users.read')) {%>
				<li class="nav-item <%= moduleName === "Người dùng" ? "menu-is-opening menu-open" : "" %>">
					<a href="#"
						class="nav-link <%= moduleName === "Người dùng" ? "active" : "" %>">
						<i class="nav-icon fas fa-users"></i>
						<p>
							Người dùng
							<i class="fas fa-angle-left right"></i>
						</p>
					</a>
					<ul class="nav nav-treeview">
						<li class="nav-item">
							<a href="/admin/users"
								class="nav-link <%= title === "Danh sách người dùng" ? "active" : "" %>">
								<i class="far fa-circle nav-icon active"></i>
								<p>Danh sách</p>
							</a>
						</li>
						<% if(permissionUtils.getPermissionUser(permissionUser, 'users.add')) {%>
						<li class="nav-item">
							<a href="/admin/users/add"
								class="nav-link <%= title === "Thêm người dùng" ? "active" : "" %>">
								<i class="far fa-circle nav-icon"></i>
								<p>Thêm mới</p>
							</a>
						</li>
						<% } %>
					</ul>
				</li>
				<% } %>
				<% if(permissionUtils.getPermissionUser(permissionUser, 'roles.read')) {%>
				<li class="nav-item <%= moduleName === "Role" ? "menu-is-opening menu-open" : "" %>">
					<a href="#"
						class="nav-link <%= moduleName === "Role" ? "active" : "" %>">
						<i class="fas fa-user-cog"></i>
						<p>
							Role
							<i class="fas fa-angle-left right"></i>
						</p>
					</a>
					<ul class="nav nav-treeview">
						<li class="nav-item">
							<a href="/admin/roles"
								class="nav-link <%= title === "Danh sách Role" ? "active" : "" %>">
								<i class="far fa-circle nav-icon active"></i>
								<p>Danh sách</p>
							</a>
						</li>
						<% if(permissionUtils.getPermissionUser(permissionUser, 'roles.add')) {%>
						<li class="nav-item">
							<a href="/admin/roles/add"
								class="nav-link <%= title === "Thêm role" ? "active" : "" %>">
								<i class="far fa-circle nav-icon"></i>
								<p>Thêm mới</p>
							</a>
						</li>
						<% } %>
					</ul>
				</li>
				<% } %>
				<% if(permissionUtils.getPermissionUser(permissionUser, 'products.read')) {%>
				<li class="nav-item <%= moduleName === "Sản phẩm" ? "menu-is-opening menu-open" : "" %>">
					<a href="#"
						class="nav-link <%= moduleName === "Sản phẩm" ? "active" : "" %>">
						<i class="fas fa-shopping-cart"></i>
						<p>
							Danh mục sản phẩm
							<i class="fas fa-angle-left right"></i>
						</p>
					</a>
					<ul class="nav nav-treeview">
						<li class="nav-item">
							<a href="/admin/products"
								class="nav-link <%= title === "Danh sách sản phẩm" ? "active" : "" %>">
								<i class="far fa-circle nav-icon active"></i>
								<p>Danh sách</p>
							</a>
						</li>
				<% if(permissionUtils.getPermissionUser(permissionUser, 'products.add')) {%>
						<li class="nav-item">
							<a href="/admin/products/add"
								class="nav-link <%= title === "Thêm sản phẩm" ? "active" : "" %>">
								<i class="far fa-circle nav-icon"></i>
								<p>Thêm mới</p>
							</a>
						</li>
						<% } %>
					</ul>
				</li>
				<% } %>
				<li class="nav-item">
					<a href="/admin/setting"
						class="nav-link <%= moduleName === "Cài đặt" ? "active" : "" %>">
						<i class="fas fa-cog"></i>
						<p>
							Cài Đặt
						</p>
					</a>
				</li>
			</ul>
		</nav>
		<!-- /.sidebar-menu -->
	</div>
	<!-- /.sidebar -->
</aside>